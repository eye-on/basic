# VEX 电控与算法知识说明

## 一、文档概述

### 1.1 文档目的

本文档不是代码注释，也不是单纯的仓库结构说明，而是一份面向新人研发成员的 VEX 电控与算法知识说明。

它的目标是让新人在接触实际比赛机器人程序时，先建立正确的整体认知，再去理解具体代码实现。重点不在“这个函数写了什么”，而在“为什么比赛机器人要这样设计”“每个模块解决什么问题”“调试时应该如何思考”。

读完这份文档后，理想状态下你应该能回答下面这些问题：

- 一台 VEX 比赛机器人为什么必须把手动、自动、传感器、执行机构分开设计
- 底盘控制里为什么要做平滑、防抖、限速
- 自动程序为什么不能只靠延时和时间控制
- 颜色、距离、惯导等传感器在比赛里的作用分别是什么
- 为什么研发电控不只是写代码，而是机械、策略、规则、调试共同约束下的系统工程

本文档会以当前仓库为背景案例，但重点始终是知识点，而不是这份代码本身。

### 1.2 规则书

规则书是所有电控设计的上位约束。

- 官方规则书：`待 @钱皓宇 补充`
- 官方 Q&A：`待 @钱皓宇 补充`
- 赛季更新说明：`建议同步补充`

为什么规则书必须放在文档前面：

- 电控不是独立学科，它服务的是比赛目标。
- 机器人能不能碰某个区域、能不能在某阶段展开机构、能不能持有多个物件、开局姿态如何摆放，这些都直接决定程序设计方式。
- 一套在实验室里“好用”的自动程序，如果违反规则，在赛场上就是无效程序。

新人最容易犯的错误之一，就是先沉迷于“把程序写复杂”，却没有先搞清楚：

- 这个赛季到底要完成什么任务
- 哪些任务得分高
- 哪些动作违规风险高
- 哪些传感器在规则允许范围内最有价值

### 1.3 阅读方式说明

这份文档建议按“先整体、后模块、再算法、最后调试”的顺序阅读：

1. 先理解电控方案为什么要分层。
2. 再理解比赛程序的常见功能模块。
3. 再理解核心控制算法解决的是什么问题。
4. 最后进入调试流程和故障排查。

如果一上来就直接看自动程序细节，通常会只记住动作顺序，而不明白背后的设计原则。

## 二、整体电控方案设计

### 2.1 机器人整体电控逻辑架构

一台 VEX 比赛机器人，从电控视角看，通常可以拆成五层：

1. 输入层
2. 状态层
3. 控制层
4. 执行层
5. 反馈层

可以这样理解：

- 输入层
  负责接收“人想干什么”或“自动程序想干什么”。
- 状态层
  负责记录“机器人现在是什么状态”。
- 控制层
  负责决定“下一步应该怎么输出”。
- 执行层
  负责让电机和机构真正动起来。
- 反馈层
  负责告诉程序“动得对不对”。

如果不做这类分层，常见后果是：

- 手动和自动逻辑混在一起
- 一个按键改动会影响很多地方
- 自动程序越写越长，没人敢动
- 传感器数据到处乱读，最终无法调试

一个成熟的比赛程序，不一定代码很多，但一定要满足两个要求：

- 结构上能让多人协作
- 调试时能快速定位问题

### 2.2 自动 / 手动模式整体设计思路

VEX 比赛程序通常要面对两种本质不同的控制方式：

- 手动模式
- 自动模式

它们最大的区别不是“有没有按键”，而是控制目标不同。

手动模式的核心目标：

- 响应快
- 手感稳定
- 不误触
- 不突然抽动

自动模式的核心目标：

- 路径可复现
- 动作可预测
- 容错可控
- 与赛场任务匹配

因此，两种模式在设计上通常会共享硬件接口，但不会共享全部控制逻辑。

常见正确思路是：

- 手动模式强调“人实时纠正”
- 自动模式强调“程序自己闭环修正”

新人常见误区：

- 认为手动能跑就说明自动也能跑
- 认为自动只要写一串动作就够了
- 认为手柄映射只是操作习惯问题，不影响程序结构

实际上：

- 手动模式可以依靠驾驶员经验补救误差
- 自动模式必须把误差控制逻辑提前写进程序里

### 2.3 赛事任务对应电控功能拆解

比赛任务在电控里，通常会被拆成一组基础能力，而不是直接写成一长串动作。

例如一个赛季常见任务，通常可拆成：

- 开局展开
- 走位
- 取料
- 识别
- 得分
- 二次取料
- 卡位或防守
- 终局动作

这些任务对应的电控能力通常包括：

- 底盘直行和转向
- 精确接近目标
- 机构吸取或夹取
- 识别颜色或距离
- 投放、发射或翻转
- 姿态切换
- 自动阶段中断和停车

这背后的知识点是：

- 比赛任务是“策略语言”
- 电控模块是“执行语言”

研发时要学会把“我要拿这个分”翻译成：

- 需要什么动作
- 需要什么传感器
- 需要什么控制精度
- 允许多大误差
- 失败后是否还能补救

### 2.4 电控整体优化方向与设计亮点

一套优秀的比赛电控，不是指“算法名词很多”，而是指下面这些能力比较平衡：

- 好开
- 稳定
- 易调
- 易改
- 不容易出事故

典型优化方向包括：

- 底盘响应更顺手
- 自动路径更稳定
- 机构动作更一致
- 传感器读数更可靠
- 调试入口更清楚
- 故障反馈更及时

电控设计真正的亮点，通常不是某个复杂控制器，而是：

- 结构清晰
- 参数集中
- 调试成本低
- 出问题时能迅速降级到可用方案

## 三、软件环境与基础配置

### 3.1 为什么统一开发环境

团队统一使用 VS Code 开发，不只是因为它常见，而是因为团队研发最怕“每个人都能写，但没有人能接手”。

统一环境的价值包括：

- 所有人看到的目录结构一致
- 编译方式一致
- 插件体系一致
- 搜索、调试、提交代码的方式一致

如果开发环境不统一，常见问题是：

- 某人本地能编，别人不能编
- 某人改了自动参数但没有同步
- 有人直接改生成文件而不是源文件

### 3.2 Git 与 GitHub 的基础能力要求

团队代码同步使用 Git 和 GitHub，这不是“加分项”，而是基础能力要求。

新人至少要掌握：

- `clone`
- `pull`
- `add`
- `commit`
- `push`
- 分支概念
- 冲突的基本处理方式

为什么电控团队必须会 Git：

- 自动程序经常需要反复试错
- 不同成员会同时改手动、自动、机构、文档
- 比赛前必须能回到一个稳定版本
- 现场应急时必须知道现在烧录的是哪一个版本

### 3.3 团队研发的基本规范

建议团队统一遵守这些习惯：

- 任何端口改动都要记录
- 任何手柄映射改动都要记录
- 任何自动参数改动都要记录
- 比赛版本和测试版本要区分
- 自动程序不要直接在主分支上乱改

从知识层面上说，这叫“可追溯性”。

一套没有可追溯性的程序，就算今天能跑，明天也未必有人知道它为什么能跑。

## 四、程序功能模块说明

### 4.1 基础驱动模块

#### 底盘驱动逻辑

底盘驱动模块，是比赛机器人程序里最基础、也最容易被低估的部分。

它的任务不是简单地“让电机转”，而是把驾驶员的操作意图转换成：

- 稳定的
- 可控的
- 可重复的

电机输出。

不同机器人可能使用：

- 差速底盘
- 全向底盘
- 麦轮底盘
- X-drive

无论底盘形式如何，底盘控制通常都会涉及四个基本问题：

- 如何把摇杆输入转换成电机输出
- 如何避免输出太突兀
- 如何保证左右动作协调
- 如何限制极端输入带来的失控风险

#### 平移、旋转、调速、缓启动缓停止

这些词背后对应的是几个基础知识点：

- 平移 / 前进后退
  表示机器人沿主要运动方向移动。
- 旋转
  表示机器人改变朝向。
- 调速
  表示在不同任务阶段给机器人不同的运动强度。
- 缓启动缓停止
  表示不让电机突然从 0 跳到最大，也不让车突然抽停。

为什么需要缓启动和缓停止：

- 机械结构有惯性
- 轮胎与地面有摩擦极限
- 电池电压会波动
- 驾驶员手指动作并不总是平稳

如果程序把摇杆输入原样直接打到底盘，常见现象是：

- 起步猛冲
- 收杆点头
- 转向发飘
- 直线时左右摆

因此，底盘程序常常会加入：

- 死区处理
- 输入平滑
- 曲线整形
- 最大速度限制

#### 底盘容错、防抖、限速逻辑

这些逻辑的本质，是在驾驶员输入和机械实际能力之间加一层缓冲。

常见目的包括：

- 防止摇杆微抖导致车乱动
- 防止操作员误碰导致机构或底盘突然动作
- 防止高速状态下转向过猛
- 防止电机输出超过合理范围

知识点上需要理解：

- 死区不是让控制变“迟钝”，而是去掉无意义噪声
- 限速不是让车变“慢”，而是让车更可控
- 平滑不是让程序“钝”，而是让动作更接近机械真实响应

### 4.2 执行机构模块

#### intake 取料机构程序逻辑

取料机构通常负责把场上的目标物吸入、夹取或传送到内部通道。

编程时需要关注几个问题：

- 吸入方向是否统一
- 多级传送是否同步
- 取料后是否需要分流
- 卡料时如何处理

取料机构在比赛里看似简单，但往往是节奏核心，因为：

- 取料效率直接决定得分节奏
- 取料稳定性决定自动能否复现
- 取料与投放之间通常要共用一部分机构链路

#### 升降 / 翻转机构控制逻辑

这类机构通常用于：

- 调整机器人姿态
- 改变得分高度
- 展开或收纳结构
- 完成终局动作

知识点重点不在某个电机怎么转，而在于：

- 动作有没有明确的状态定义
- 不同状态之间能不能安全切换
- 动作是否需要互锁
- 动作过程中是否允许驾驶员中断

如果没有状态设计，常见问题是：

- 同一按键在不同阶段含义混乱
- 机构切换到一半又被另一动作打断
- 自动和手动控制争抢同一电机

#### 弹射 / 投放机构控制逻辑

投放机构的核心问题通常是：

- 出料方向是否稳定
- 出料速度是否一致
- 多个电机是否同步
- 投放后是否需要自动复位

这类机构最容易暴露“机械和程序不匹配”的问题。

程序角度上常见做法：

- 固定速度模式
- 分档模式
- 状态机模式
- 传感器触发模式

其中状态机思路尤其重要，因为它能把“预装填、待机、发射、回位”这些过程组织得更清晰。

#### 其他自定义执行机构功能说明

每个赛季都会有特殊机构，所以执行机构设计一定要留出扩展空间。

新人需要理解：

- 不要把每个机构都当成独立的小玩具
- 机构之间常常是联动关系
- 真正好维护的程序不是“一个机构一个 if”，而是有统一状态和统一控制思想

### 4.3 传感器数据处理模块

#### 陀螺仪姿态解算、角度校准、归零逻辑

惯性传感器是自动程序里最关键的传感器之一。

它主要解决的问题不是“知道朝哪边”，而是：

- 机器人转了多少
- 当前朝向是否偏了
- 自动转向是否到位
- 直线运动时是否跑偏

知识点上需要理解三件事：

1. 校准
   传感器在启动后需要稳定参考系。
2. 归零
   程序需要一个统一的起始角度基准。
3. 姿态使用
   后续所有转向和纠偏都依赖这个角度基准。

如果惯导校准不好，常见结果是：

- 自动转向角度不准
- 直线保直失败
- 路径越跑越偏

#### 编码器位移、速度计算逻辑

编码器的意义，是让程序知道“轮子实际转了多少”，而不是只知道“我命令它转了多少”。

这两者差别非常重要。

程序命令电机转，不代表机器人真的按照预期移动，因为：

- 地面可能打滑
- 轮胎可能磨损
- 机械负载可能变化
- 电池电量可能下降

因此，编码器常用于：

- 测量位移
- 估计速度
- 统计走过的距离
- 辅助自动定位

#### 距离 / 触碰 / 颜色传感器数据读取与滤波处理

不同传感器解决的问题不同：

- 距离传感器
  适合靠近目标、贴边、定距。
- 触碰传感器
  适合确认是否撞到极限位、是否到位。
- 颜色传感器
  适合区分不同目标物、判断物体通过状态。

新人要理解一个重要原则：

- 传感器数据永远不是“真理”
- 传感器数据是“带噪声的参考”

因此实际程序里常需要：

- 多帧确认
- 容差判断
- 异常值过滤
- 超时保护

如果直接把单次读数当成绝对判断依据，程序很容易抖动、误判或卡死。

### 4.4 手动操控模块（遥控模式）

#### 遥控器按键、摇杆功能映射表

手柄映射不是简单的“哪个键干什么”，而是整套人机交互设计。

好的映射应满足：

- 常用动作放在最顺手的位置
- 高风险动作不容易误触
- 同一类动作的按键风格一致
- 驾驶员在高压环境下也容易记住

设计映射时应考虑：

- 操作员左右手分工
- 比赛节奏
- 哪些动作需要同时进行
- 哪些动作需要边开车边完成

#### 手动操控逻辑、快捷键功能说明

手动模式的知识点重点包括：

- 连续控制
  例如底盘前进、转向。
- 离散触发
  例如模式切换、一次性展开。
- 长按逻辑
  例如维持某动作直到松手。
- 快捷组合
  例如快速进入某个比赛阶段状态。

程序设计时要明确区分：

- “按一下切换一次”
- “按住就持续动作”
- “必须长按才触发”

如果这些语义混乱，驾驶员很难建立肌肉记忆。

#### 手动模式防抖、误触规避设计

手动防抖不仅是程序细节，它直接影响比赛稳定性。

常见方法包括：

- 单次按下边沿触发
- 摇杆死区
- 长按触发
- 双键确认
- 模式切换冷却时间

这些设计的目的，是减少“操作员本来不想这样做，但机器人还是做了”的情况。

### 4.5 自动程序模块（Autonomous）

#### 自动程序整体流程框架

自动程序本质上不是“一串动作”，而是一套受目标、传感器、时间和容错约束的任务执行系统。

通常可分为两层：

- 能力层
  负责提供“前进、转向、定距、投放、展开”等基础动作。
- 流程层
  负责按比赛策略把这些动作组合起来。

如果没有这种分层，自动程序会很快变成：

- 一长串难以维护的顺序语句
- 不可复用
- 无法局部调试

#### 各阶段任务逻辑

自动程序一般可以按比赛目标拆成若干阶段：

- 开局走位
- 接近目标
- 取料
- 识别和筛选
- 得分
- 二次任务
- 防守或终局

每一阶段都要考虑：

- 成功条件是什么
- 允许误差是多少
- 如果失败怎么办
- 是否需要下一阶段继续补救

这才叫“任务逻辑”，而不是只记住一串移动距离。

#### 自动程序容错、中断、复位逻辑

真正能上场的自动程序，必须考虑失败场景。

常见容错设计包括：

- 超时退出
- 条件不满足则跳过
- 动作失败后停车
- 进入备用流程

为什么自动必须能中断：

- 传感器可能临时失效
- 机械可能卡住
- 场地摩擦可能变化
- 对抗环境下机器人可能被撞偏

一套完全没有容错的自动，实验室里可能很“丝滑”，赛场上往往很脆弱。

#### 多套自动程序方案区分与适用场景

成熟的比赛队伍通常不会只准备一套自动。

常见方案会按这些维度区分：

- 稳定优先
- 得分优先
- 防守优先
- 特定起始位置优先
- 测试专用

这背后的研发知识点是：

- 自动不是越复杂越好
- 自动要服务当场比赛策略
- 备用方案的价值往往比主方案更高

### 4.6 辅助功能模块

#### 屏幕数据显示

屏幕显示的意义不是“看起来高级”，而是降低调试成本。

适合显示的常见内容包括：

- 当前角度
- 当前模式
- 自动阶段
- 电池状态
- 球数或目标数
- 传感器状态

一行好的状态显示，常常能省掉很多盲目排查时间。

#### 日志打印、数据调试功能

日志和调试输出的本质，是把机器人运行过程“可视化”。

常见用途包括：

- 记录自动过程中每一步状态
- 输出传感器数据
- 输出目标值和实测值
- 导出调参数据

没有日志时，很多问题只能靠猜。

#### 故障自检、异常提示功能

这是很多新队伍最容易忽略的部分。

比赛程序不应只考虑“正常情况怎么跑”，还应考虑“哪里坏了我怎么知道”。

典型自检内容包括：

- 电机是否连接
- 关键传感器是否在线
- 惯导是否校准完成
- 电池是否过低
- 机构是否处于安全初始状态

## 五、核心算法与控制逻辑

### 5.1 PID 控制参数与调参说明

PID 是比赛机器人中最常见的闭环控制思想之一。

它要解决的问题很简单：

- 目标和实际之间有误差
- 程序需要根据误差持续修正输出

三个部分的直观理解：

- P
  误差越大，修正越强
- I
  长时间累积的小误差也要被修掉
- D
  误差变化太快时，提前抑制过冲

新人最容易误解的是：

- 以为 PID 是“万能高级算法”
- 以为只要调 PID，机械问题也能被程序救回来

正确理解应该是：

- PID 是一种误差修正框架
- 它是否好用，高度依赖机械稳定性和传感器质量
- 参数调不好时，PID 反而会放大问题

调参基本原则：

- 先保证机械结构稳定
- 先只调 P
- 再视情况补 I
- 最后才考虑 D
- 每次只改一个方向

### 5.2 底盘定位、纠偏算法逻辑

定位的本质，是让程序知道“我现在大概在哪里”。

纠偏的本质，是让程序知道“我偏了多少，应该怎么拉回来”。

比赛里常见的定位与纠偏来源包括：

- 编码器
- 惯导
- 距离传感器
- 场地边界或机械接触参考

常见思路：

- 直线靠编码器估计距离
- 转向靠惯导估计角度
- 接近目标时用距离传感器收尾
- 通过多个信息源综合判断是否到位

知识点重点不是数学公式本身，而是：

- 单一传感器往往不够稳定
- 多传感器互补通常更可靠
- 纠偏一定要设置容差和上限

### 5.3 自动任务精准控制算法

自动精准控制的核心不是“绝对精确”，而是“在比赛需要的精度范围内稳定复现”。

常见实现方法包括：

- 距离闭环
- 角度闭环
- 位姿闭环
- 前视点控制
- 激光或墙面定距

需要理解的核心原则：

- 不同动作对精度要求不同
- 不必对所有动作都追求极高精度
- 关键得分动作要重点保精度
- 过度追求复杂控制，可能降低整体稳定性

### 5.4 防抖、滤波、限速等优化算法

这些优化算法往往不“炫”，但非常实用。

它们解决的是比赛里最常见的真实问题：

- 摇杆抖
- 传感器噪声
- 电机响应突变
- 机械惯性过大

常见方法包括：

- 一阶滤波
- 移动平均
- 输入限幅
- 输出斜坡
- 容差判断
- 边沿触发

从工程角度看，很多时候这些基础优化带来的收益，远大于引入一个复杂但不稳定的新算法。

## 六、调试流程与标准

### 6.1 硬件单项调试流程

调试的第一原则是：

- 先分模块
- 再联调

硬件单项调试建议顺序：

1. 单独检查每个电机方向和端口
2. 单独检查每个机构能否动作
3. 单独检查惯导是否正常校准
4. 单独检查距离、颜色、触碰等传感器
5. 最后再做整机联调

为什么不能一上来就整车跑：

- 出问题时无法判断是谁的问题
- 机械和程序问题会互相掩盖

### 6.2 手动模式整体调试标准

手动模式调试应重点检查：

- 摇杆回中是否稳定停车
- 前后左右是否符合驾驶员直觉
- 常用机构能否顺畅切换
- 高风险动作是否容易误触
- 连续驾驶时车是否容易失控

标准不只是“能动”，而是：

- 操作员能不能稳定复现同样动作

### 6.3 自动程序分步调试、整体联调流程

自动程序调试应坚持“小步快跑”原则。

推荐顺序：

1. 先调直线
2. 再调转向
3. 再调定距
4. 再调单个机构动作
5. 再组合成小流程
6. 最后再跑完整自动

不要在完整自动里同时调十个参数，否则你最后根本不知道是哪一个起了作用。

### 6.4 PID 参数调试步骤与记录标准

调参本身也是研发流程的一部分，不能靠记忆。

建议每次调参都记录：

- 日期
- 调试人
- 场地情况
- 电池状态
- 改了哪些参数
- 改之前和改之后的效果
- 是否保留本次修改

如果不做记录，团队很容易陷入：

- 今天觉得更好了
- 明天又觉得变差了
- 最后没人知道哪个版本真正最好

### 6.5 赛前整机调试、复刻校准流程

比赛前最重要的不是“临时大优化”，而是“保证稳定复现”。

赛前应固定执行：

1. 检查硬件连接
2. 检查电池
3. 校准惯导
4. 确认初始姿态
5. 跑短流程验证方向
6. 跑正式自动验证稳定性
7. 锁定版本和参数

这套流程的意义，在于把偶然性尽量降低。

## 七、常见问题与故障排查手册

### 7.1 硬件故障

常见硬件故障包括：

- 电机不转
- 电机方向反
- 传感器无响应
- 接线松动
- 机构卡滞

排查原则：

- 先查电源和连接
- 再查端口和方向
- 再查程序逻辑

不要一看到“不动”就先怀疑算法。

### 7.2 程序故障

常见程序故障包括：

- 自动走偏
- 转向过头
- 模式切换混乱
- 某机构反复触发
- 程序中途卡死

知识点上要学会分辨：

- 是逻辑错误
- 是参数不合适
- 是传感器不稳定
- 还是机械状态变化导致的结果变化

### 7.3 操控异常、精准度不足问题排查

如果驾驶员反馈“车不好开”，可能原因包括：

- 摇杆死区设置不合理
- 平滑过强或过弱
- 机械重心变化
- 电池状态差
- 底盘左右输出不一致

如果自动不准，可能原因包括：

- 惯导未校准好
- 轮胎打滑
- 参数过激
- 距离传感器误差大
- 初始位置摆放不一致

真正成熟的排查思路，是把问题拆成：

- 输入问题
- 控制问题
- 执行问题
- 反馈问题

### 7.4 赛事突发问题应急处理方案

比赛现场最重要的能力之一，是快速降级而不是盲目硬改。

建议提前准备：

- 一套最稳的保守自动
- 一套简化版自动
- 一套纯手动兜底策略
- 一份关键参数速查表
- 一份硬件快速检查清单

现场应急原则：

- 优先保证不犯规
- 优先保证基础得分
- 优先保证可控性
- 不在最后时刻做结构性大改

## 结语

VEX 电控研发的核心，不是把代码写得多复杂，而是把“规则、策略、机械、程序、调试”这五件事连接起来。

真正有价值的新人培养，不是让大家死记某份代码，而是让大家理解：

- 为什么模块要分开
- 为什么自动要闭环
- 为什么调试必须分步骤
- 为什么很多问题不是单纯的程序问题

当前仓库可以作为一个实际案例去阅读，但阅读代码的正确方式应该是：

- 先理解知识点
- 再观察这份代码是如何落地这些知识点的
- 最后再思考哪些地方还能优化

这样你学到的就不是“一份项目文件”，而是一套以后还能迁移到其他赛季、其他机器人上的研发方法。
