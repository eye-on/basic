#include "hardware/bed/autonomous.h"

namespace basic::hardware::bed::autonomous {

void run_routine(
    basic::hardware::bed::RobotHardware& hardware,
    basic::hardware::bed::RobotState& state,
    vex::competition& competition) {
  (void)hardware;
  (void)state;
  (void)competition;
  // TODO(bed): 接线后在此编写自走例程
}

}  // namespace basic::hardware::bed::autonomous
